import { generateMetadata as generateSEO } from '@/lib/seo'

export const metadata = generateSEO({
  title: 'Login - GridNexus',
  description: 'Sign in to your GridNexus account to manage your web development projects',
  noIndex: true,
})
