import { generateMetadata as generateSEO } from '@/lib/seo'

export const metadata = generateSEO({
  title: 'Client Dashboard - GridNexus',
  description: 'Manage your web development projects, track progress, and communicate with developers',
  noIndex: true,
})
